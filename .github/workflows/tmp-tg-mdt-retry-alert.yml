name: Patch Telegram MDT retry alert

on:
  push:
    branches:
      - "work/tg-mdt-retry-alert"
    paths:
      - ".github/tg-mdt-retry-alert.trigger"

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          ref: work/tg-mdt-retry-alert
      - uses: actions/setup-python@v6
        with:
          python-version: '3.12'
      - name: Apply patch
        run: python scripts/_patch_tg_mdt_alert.py
      - name: Install test dependencies
        run: python -m pip install -r requirements-dev.txt
      - name: Run full tests
        env:
          PYTHONPATH: ${{ github.workspace }}
        run: pytest -q
      - name: Commit clean patch
        shell: bash
        run: |
          rm -f .github/workflows/tmp-tg-mdt-retry-alert.yml
          rm -f .github/tg-mdt-retry-alert.trigger
          rm -f scripts/_patch_tg_mdt_alert.py
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git diff --cached --check
          git commit -m "Alert on stale Telegram MDT retry queue"
          git push origin HEAD:work/tg-mdt-retry-alert
