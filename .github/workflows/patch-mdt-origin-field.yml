name: Patch MDT origin field

on:
  push:
    branches: [fix/mdt-origin-field]

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          ref: fix/mdt-origin-field
          fetch-depth: 0
      - uses: actions/setup-python@v6
        with:
          python-version: '3.12'
          cache: pip
      - name: Install test dependencies
        run: pip install -r requirements-dev.txt
      - name: Apply patch and test
        run: |
          set -Eeuo pipefail
          python scripts/patch_mdt_origin_field.py
          pytest -q tests/test_shared.py -k 'create_lead'
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add shared/mdt.py tests/test_shared.py
          if git diff --cached --quiet; then exit 0; fi
          git commit -m "Preserve VK origin in MDT lead"
          git push origin HEAD:fix/mdt-origin-field
